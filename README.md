## OAuth Demo

### Backend Setup
```bash
cd backend
npm install
npm start
```

### Frontend Setup
```bash
cd frontend
npm install
npm run dev
```

Before running the application, ensure the following environment variables are set: MONGODB_URI, CLIENT_ID, CLIENT_SECRET, and ENCRYPTION_KEY.